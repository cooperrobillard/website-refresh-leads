"""Scoring modules for evaluating refresh potential.

Deterministic scoring is preserved under ``app.scoring.deterministic`` while
legacy import paths stay available during the hybrid transition.
"""
