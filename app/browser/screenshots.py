"""Placeholder screenshot helpers."""

from __future__ import annotations

from pathlib import Path


def capture_screenshot(url: str, output_path: Path) -> Path:
    """Return the intended screenshot path without capturing anything yet."""
    # TODO: Implement Playwright screenshot capture in a later phase.
    return output_path
